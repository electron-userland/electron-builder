---
"app-builder-lib": major
"electron-updater": major
---

chore: remove `electronUpdaterCompatibility` config (auto-detect from `dependencies['electron-updater']`); drop deprecated `UpdateCheckResult.versionInfo` and `BT_TOKEN` (Bintray) publish auto-detect (BREAKING)
